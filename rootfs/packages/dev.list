# On top of standard.list: a native toolchain. Adds roughly 400MB, so this is
# an SD-card profile; it will not fit the 237MB UBI volume in the NAND.
build-essential
pkg-config
cmake
gdb
python3-dev
python3-pip
python3-numpy
vim
